1. git init - 시작 .git 생성
2. git add 
    untracke-> traked  로 변경 
3. git commit -m " "
    커밋했을때는 변경된 파일만 변경점 저장, 변경되지 않은 파일은 변경없음을 저장하는 원리.

4. git push

5. git branch cat - 브런치를 만들라는 의미
6. git checkout -브런치를 옮겨가기 

싫어요